node_modules/
dist/
src-tauri/target/
src-tauri/gen/schemas/
*.local
.DS_Store
test-results/
playwright-report/
shots-menu/
.claude/
# Chave privada do updater. Se ela vazar, qualquer um assina um "update"
# que os JARVIS instalados aceitam como legítimo. Mora fora do repo
# (~/.jarvis/updater.key); este padrão é só a rede de segurança.
*.key
*.key.pub
# Gerado por `npm run release:manifest` a cada versão; a cópia que vale é a
# anexada à release do GitHub.
latest.json
# Saida do `vite build -c vite.mobile.config.ts` (o PWA do celular), embutida
# no executavel pelo `include_bytes!`. Gerada a cada build; a fonte esta em
# `src/mobile/`.
src-tauri/webapp/
